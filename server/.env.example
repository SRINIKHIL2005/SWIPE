PORT=5050
# You can set either GEMINI_API_KEY or GOOGLE_API_KEY. Both are treated the same.
# Use a Google AI Studio (Gemini) API key (starts with AIza...). No quotes or spaces.
GEMINI_API_KEY=
# GOOGLE_API_KEY=

# Optional: override model id
GEMINI_MODEL=gemini-1.5-pro-latest

# Force Google Generative AI API version. v1 is recommended.
GEMINI_API_VERSION=v1
