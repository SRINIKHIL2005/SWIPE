PORT=5050
GOOGLE_API_KEY=
# Use a valid Google AI Studio API key (starts with AIza...). No quotes or spaces.

# Optional: override model id
GEMINI_MODEL=gemini-1.5-pro-latest

# Force Google Generative AI API version. v1 is recommended.
GEMINI_API_VERSION=v1
